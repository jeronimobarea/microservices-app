# Hace run de la imagen en un contenedor
docker run -d -p 5000:5000 <nombre de la imagen>:latest

# Mata un contenedor
docker kill <id o nombre>

# Muestra las imágenes
docker images

# Muestra los contenedores activos
docker ps

# Muestra los contenedores parados
docker ps -a

# Build de una imagen
docker build -t <nombre>:<version> .